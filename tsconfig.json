{
  // Editor / language-server default project. CI gates use tsconfig.build.json
  // (shipped source), tsconfig.test.json (tests + tooling) and
  // tsconfig.preview.json (apps/) as separate projects; this file exists so
  // opening any file in an editor resolves against the same base options.
  "extends": "./tsconfig.test.json"
}
