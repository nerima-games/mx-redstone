---
---

No release: a test comment correction, no source or behaviour change. Consumers receive identical
bytes. An explicit no-release record rather than silence, because the changeset gate exempts only
documentation and workflow paths — a `test/` diff is not exempt even though it ships nothing.
